import { IconStateDirective } from './icon-state.directive';

describe('IconStateDirective', () => {
  it('should create an instance', () => {
    const directive = new IconStateDirective();
    expect(directive).toBeTruthy();
  });
});
